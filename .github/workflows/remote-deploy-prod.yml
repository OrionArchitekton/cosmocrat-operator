# ⚠️ WORKFLOW DISABLED - REPOSITORY QUARANTINED
# See README_QUARANTINED.md for details
# Date: 2026-01-20
# Reason: R1 Operator Plane Split - authority isolation

name: Remote Deploy Prod (DISABLED)

on:
  workflow_dispatch:

jobs:
  run-remote-deploy:
    name: Deploy Remote Prod
    runs-on: ubuntu-latest
    if: false  # QUARANTINED: Deployments disabled
    permissions:
      contents: read
    steps:
      - name: Dispatch prod remote deployment workflow
        uses: peter-evans/repository-dispatch@v3
        with:
          token: ${{ secrets.REMOTE_DEPLOYMENT_TOKEN }}
          repository: BloopAI/vibe-kanban-remote-deployment
          event-type: vibe-kanban-remote-deploy-prod
          client-payload: |
            {
              "ref": "${{ github.ref_name }}",
              "sha": "${{ github.sha }}"
            }
